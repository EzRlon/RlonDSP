# Changelog

本项目所有值得记录的变更都会写入本文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 修复

- 修复实时音效预设区「新建行」被误认为是一条坏预设的问题：该行现在只保留名称输入与「＋ 新建预设」标记，不再出现用不了的重命名 / 删除 / 开关。
- 修复弹出窗口（设置 / 实时音效 / 脉冲反馈）滚到最底部时，最后一行内容被底部播放栏盖住的问题。根因是弹窗所在的层级与播放栏同级，而播放栏在文档中更靠后，于是后绘制的播放栏压在了弹窗上面。现已调整层级关系（内容区高于播放栏、标题栏保持最高），弹窗铺满整窗，遮罩也同步改为与窗口一致的圆角。
- 修复频谱区所有文字发虚的问题。根因有两处：一是画布内部只有浏览器默认的 300×150 像素，被拉伸到卡片实际尺寸后字被拉糊；二是卡片宽度常为 247.5 这类小数，1 倍渲染会出现半像素错位。现改为按显示尺寸的 **2 倍分辨率**绘制再缩回（247.5 × 2 = 495，正好是整数），小字边缘明显干净，播放实测 77 帧/秒无掉帧。
- 修复频谱各卡片底部小字被卡片圆角裁掉的问题（频谱质心、倍频程频段、相位分析、弧形电平表、频谱分析仪、示波器）。根因是小字基线原本只距下沿 3～5 像素、距左右 3～4 像素，正好落在 16 像素圆角被剪掉的区域里。现已统一改为距下沿 9 像素、左右内缩 18 像素，频谱质心的色带与小字也重新排布（小字 → 留 6 像素 → 色带）。
- 修复实时音效「预设 → 保存」点了没反应的问题。原因是保存用了 `window.prompt` 弹窗输入名称，而 Electron 中该弹窗不可用（直接返回空值），代码随即判定为「未输入名称」而放弃保存。现已改为卡片内的名称输入框，并新增「重命名」；脉冲列表的「重命名」也是同一原因失效，同步改为就地编辑（回车或失焦提交，Esc 取消），代码中的 `window.prompt` 已全部清除。
- 修复「空间音效制作器 → 图示均衡」中推子无法用鼠标准确定位的问题。推子是把一个 132×132 的正方形滑块旋转 90° 做成的，导致它的**可点击区域是 132 像素宽的方块**，而相邻推子间距只有 60 像素——点击区互相重叠一倍多，点下去会落到旁边的推子上。现已把可点击区域压到与实际显示一致的宽度（24 像素），并用上下外边距补回旋转溢出的高度，外观与布局保持不变。实测点击落点判定已正确命中目标推子。
- 修复播放列表与频谱展示区观感不一致的问题（左侧像一块死白、右侧却是通透玻璃）。根因是系统开启了「减少透明效果」，而降级规则只作用于 `.lg-*` 元素，直接引用令牌的 `.viz-card` 等表面没有跟着变化。现已把降级逻辑改为统一覆盖玻璃令牌，所有表面必定同步变化；同时下调内容层不透明度，使歌曲列表与频谱卡片色调一致（实测像素偏差由 42 降到 5）。
- 修复设置窗口中「主题」「语言」「输出设备」等下拉框在深色模式下选项文字看不见的问题。下拉选项弹层由浏览器绘制，背景色不随页面主题变化，而文字颜色会被继承，导致深色主题下出现「浅字浅底」。现在为选项显式指定了两种主题下的底色与文字色。
- 修复脉冲反馈（RLON-空间音效制作）界面中参数标签发虚的问题。标签被更具体的选择器挡住，一直沿用复古机架的 9px 细体等宽字，在玻璃底上笔画发虚；现已改回界面字体并调到 10px，同时提高次文字与背景的对比度（深色 #9098a8 → #c3cad6，浅色 #6d7483 → #4d5665）。
- 修复频谱质心面板中局部变量遮蔽国际化函数，导致绘制过程抛出异常、可视化渲染循环中断且无法自动恢复的问题。播放时 10 路可视化现在可持续正常刷新。
- 修复实时音效均衡器的圆形滑块与竖条轨道未对齐的问题（原先整体右偏 5 像素）。
- 修复脉冲反馈均衡器的圆形滑块与竖条轨道未对齐的问题（原先整体左偏 1 像素）。
- 修复脉冲反馈界面中部分英文标签只翻译一半的问题（例如信号链标签显示为「SIGNAL 信号链」）。根因为语言表按先后顺序匹配，短词抢先命中长词组。

### 变更

- **实时音效统一为一个「保存」+ 一个「重置」**：移除每个效果卡里的保存入口与预设条目上的保存按钮，顶部 `[保存] [重置]`（保存位于重置左侧）成为唯一入口。保存写入完整状态（各效果开关、参数、差分环绕、空间音效等）；若有已开启的预设则覆盖它，否则按名称新建 —— 复用同一个 Preset 存储，没有第二套保存系统。
- **所有效果卡统一支持折叠 / 展开**（均衡器除外，它常驻展开）。折叠只影响界面显示，开关才真正进出统一 DSP 图谱，两者严格分离；折叠不会丢参数，默认除预设与均衡器外全部收起。
- **均衡器新增 8 种风格预设按钮**（默认 / 古典 / 重金属 / 流行 / 爵士 / 民谣 / 摇滚 / 电子，仅此 8 种）。点击即把 10 段 EQ 调成对应曲线，当前生效的风格点亮；手动拖动滑杆后高亮自动取消。实测把「重金属」调出后低频能量上升、中频下降，与曲线方向一致。
- **新增 9 个真正可工作的效果器**，全部追加在「实时音效」列表末尾，使用与既有 DSP 完全相同的卡片 / 开关 / 折叠 / 参数行，不新增任何 UI 风格：
  1. **动态 EQ**（频率 / Q / 增益 / 阈值 / 作用范围 / 启动 / 释放）—— 该频段超过阈值时按比例施加设定增益；
  2. **多段压缩**（低/高频分频、阈值、压缩比、启动、释放、三段增益补偿）—— 互补分频后三段各自压缩再求和；
  3. **去齿音**（频率 / 阈值 / 作用范围 / 启动 / 释放）—— 检测齿音频段并整体衰减；
  4. **扩展器**（阈值 / 扩展比 / 启动 / 释放 / 作用范围）—— 低于阈值按比例继续压低，是噪声门的升级版；
  5. **瞬态整形**（起音 / 延音 / 干湿比 / 输出）—— 快慢包络之差决定起音的强化与延音的削弱；
  6. **削波 / 饱和**（驱动 / 软硬模式 / 阈值 / 上限 / 干湿比 / 输出）；
  7. **延迟 / 回声**（时间 20–1000 ms、反馈、干湿、反馈滤波、乒乓）；
  8. **合唱**（速率 / 深度 / 干湿 / 左右错开）；
  9. **镶边**（速率 / 深度 / 反馈 / 干湿）。
  全部作为独立节点接入统一 DSP 图谱（延迟与尾音如实上报），参数带安全夹取，非法值（NaN / Infinity / 越界）不会进入音频路径。
- **移除底部卡片下方的投影与深色带**：底部播放区原先是一张带外投影的悬浮卡片，卡片四周（左右下各 14 像素）会露出比卡片更深的窗口底色，其中投影又被窗口下沿截断，形成一条突兀的深色横带。现已去掉该投影，并把播放区改为**通栏贴底**，深色带彻底消失。
- **窗口四角改为圆角**：窗口本身无系统边框，Windows 不会自动磨圆角，因此把窗口底做成透明，由界面用 12 像素圆角裁出整块内容（迷你播放器为 10 像素）。最大化时自动恢复直角；拖动边缘改变窗口大小时圆角保持不变。弹窗遮罩与最底层毛玻璃背景同样跟随圆角裁剪。
- **左侧播放列表透出专辑封面颜色**：列表区原先自己又铺了一层白色底（内容层玻璃），与左栏上方的工具栏叠加后整个左栏被「洗白」，封面颜色透不过来，列表区与工具栏之间还出现一条深浅分界。现已去掉这层白底与描边，左栏恢复为一整块通透毛玻璃，封面颜色与其它区域一致地透出（实测由 204,250,255 变为 122,245,255，与标题栏一致）。
- **实时音效面板各功能区块加了分割线**：原先各区块只有一层近乎透明的白色描边，浅色背景下几乎看不见，区块之间糊在一起；现在在区块间距中补了一条实色细线，每个功能区一目了然。
- **移除歌词功能**：删除播放控制栏的歌词按钮与相应图标（桌面歌词显示也一并停止）。移除后播放栏右侧的按钮自动重新排列，不留空位。
- **外部加载的脉冲进入列表**：通过「加载脉冲」从磁盘载入脉冲文件后，会自动在下方列表新增一条，与制作器传送过来的脉冲同级 —— 可重命名、可删除、可用开关启用，名称后面带「外部加载」标识以示区分（只记录文件路径，不复制文件内容）。
- **预设区改为「下拉 + 当前预设卡片」**：保存、重命名、删除三个操作收进下拉框下方的次级卡片，卡片内含名称输入框。未选中预设时名称框预填默认名称 `RlonIR_`（可直接保存为新预设）；选中预设后显示其名称，保存为覆盖更新。顶部工具栏保留重置、导出、导入。
- **关于页面改写为常规开源项目表述**：不再写「本项目基于 Echomusic 分支改造」，改为先介绍产品本身，再在「开源致谢」中**逐条列出真正参考的部分**（主界面框架与功能分区、原生音频模块的调用接口设计），并说明 Echomusic 的参考源码以只读形式保留、不参与构建与运行，其余（界面、可视化、脉冲反馈生成器、空间音效、统一 DSP 图谱）均为本项目自行实现。第三方依赖表同步移除 Echomusic（它并非运行时依赖）。
- **界面改名**：实时音效中的「IRS 空间音效」改为「空间音效」（英文 Spatial Audio）；脉冲反馈中的「RLON-空间音效制作」改为「空间音效制作器」（英文 Spatial Audio Studio）；其副标题由「脉冲响应处理器 · 型号 1200」改为「由 Rlon 音频驱动」（英文 Powered by Rlon）。两处名称均跟随软件语言切换。
- **播放列表条目改为通栏**：歌曲条目原先缩进并带圆角，看起来像一张张独立小卡片且显示不完整。现在条目铺满整行、去掉圆角，并用细分隔线区分相邻条目。
- **专辑封面作为最底层背景**：当前歌曲的封面会作为整个窗口的最底层背景，由上层遮罩做高强度高斯模糊（blur 60px + 饱和度提升），形成类似手机系统状态栏/控制中心的毛玻璃底。没有封面时自动回退为原本的渐变背景；深浅主题各自叠加不同的压暗/提亮遮罩，不影响主题切换。
- **弹窗底层改为毛玻璃遮罩**：所有弹出窗口（设置、实时音效、关于）的底层遮罩由原来的纯色半透明改为「半透明 + 高斯模糊（blur 14px）」，背后内容整体虚化以突出弹窗。遮罩底色跟随主题：浅色为白色薄雾，深色为黑色压暗；不支持背景模糊的环境会自动改用更高不透明度的底色兜底。
- **播放列表与频谱展示区改为平铺布局**：两个功能区原先做成悬浮卡片，四周与中间共有 14 像素留白，底部还有圆角。现已去掉留白与圆角，贴合窗口边缘自上而下铺满，两块之间只用一条细分隔线区分。
- 播放列表补齐与频谱展示区一致的淡紫蓝渐变底，避免该区域看起来是一块死白的纯色块（两侧底色本来就相同，差别只在展示区内部多了一层渐变）。
- 移除设置窗口中与"关闭时最小化到托盘"功能重复的"最小化时到托盘"选项，保留前者。
- 脉冲反馈的信号链改为单行完整显示，WAV 环节不再折行。
- 精简脉冲反馈「构建状态」中 P1 条目的说明文字，使其单行显示。
- 均衡器频点改为 ISO 八度系列：31 / 62 / 125 / 250 / 500 / 1k / 2k / 4k / 8k / 16k Hz。
- 均衡器参数变化加入平滑过渡，拖动滑块不再产生爆音。
- 限幅器升级为前瞻限幅：前瞻窗口最大值 + 立体声联动 + 峰值保护，正常情况不再依赖削波；新增「前瞻」「释放」两个可调参数。
- 实时音效面板布局调整：各效果开关从「总增益」下方的集中开关区，下放到对应子卡片的标题栏右侧（与标题两端对齐），并移除原集中开关区。超高频净化原本没有子卡片，已为它新增独立卡片。

### 移除

- 移除已过时的内部验收报告 `9项改造完成校验报告.md`（内容与实际界面不符，且会被打包进成品）。

### 新增

- **设置 → 版本更新**：完整的系统更新入口，外观沿用设置窗口既有样式（卡片 / 行 / 按钮 / 玻璃令牌），完全跟随主题与语言。
  - **检查更新**：主进程真实访问项目的正式 GitHub Release（不使用 main 分支提交），返回版本号、发布时间、更新说明与更新包信息；**失败时如实报错**（网络不可用 / 更新源无正式版本），绝不把「连不上」当成「已是最新」。
  - **版本比较**：按语义化版本规则比较，只有远程更新才提示；同版本或更旧版本一律不提示（不会出现降级更新），版本号解析不了按失败处理。
  - **更新说明**：只作为纯文本显示（先清空再用 textContent 写入），远程内容不会被当作 HTML/脚本执行。
  - **下载**：真实进度（已下载 / 总大小 + 百分比），可随时取消；取消或失败都会清理临时文件，**不会破坏当前已安装的版本**。
  - **校验**：下载时同步计算 SHA-256；若 Release 附带了 `SHA256SUMS.txt` 之类的校验文件，则比对官方摘要，不一致立即停止更新。
  - **安装**：先下载 → 再校验 → 最后才交给安装程序；已安装版（装在 `%LOCALAPPDATA%\RlonDSP`）会启动安装程序完成升级，**便携版不支持自动安装**，界面会明确说明并提供「打开发布页」入口（不伪造能力）。
  - **自动检查更新**：默认开启，启动后延迟在后台检查，只提示、不自动安装、不重启、不打断播放；开关随设置一起保存。
  - **与音频完全解耦**：检查/下载不修改 DSP 图谱、不改音量、不改播放状态、不改预设（已实测播放中检查更新，播放不中断、参数与预设不变）。

- **差分环绕（声道延迟 / Haas 效应）**：可把左或右声道整体延后 0～30 ms，制造左右时间差，从而改变人声定位（先发声的一侧被优先感知）。控制面板位于「实时音效 → 脉冲反馈（RLON-空间音效制作）」中，样式与面板内其它模块完全一致；它是**实时模块**，调整后立即作用于当前播放，不参与 IR 离线渲染。
- 差分环绕已作为 `channel-delay` 节点类型接入统一 DSP 图谱，具备独立的启用开关、参数与延迟统计。

- **统一 DSP 图谱**（`src/dsp-host.js`）：所有 DSP —— RlonDSP 内置、原生引擎、第三方 Provider、空间音效、卷积、分析器 —— 都是同一张图里的**独立节点**，可以同时启用、按图里的顺序共同工作。
  - **同一种功能允许存在多个节点**（例如两个均衡器），由用户明确加入；只要加进图里就允许串联执行。
  - 只有启用且未旁通的节点才进入执行计划，**每个节点恰好执行一次**；用户没加进来的实现绝不执行，不存在隐式重复处理。
  - 节点自带完整元数据：id、type、owner、implementation、enabled、bypass、parameters、通道要求、延迟帧、尾音帧、首选块大小、实时安全性、能力标签。
  - **延迟如实统计**：串联相加、并行取最长分支，并给出短分支需要补偿的帧数。
  - **尾音（Tail / Drain）**：混响、卷积、空间音效等节点被标记为需要 drain，停止 / 切歌 / 跳转时可据此处理。
  - **并行分支**：节点可标注分支标签，相邻的同段落节点组成并行组，经混合器汇入后续节点。
  - Provider 与内置实现**并存不互斥**：多个 Provider 可以提供同类型实现，用户加哪个执行哪个；Provider 出错时只摘掉它自己的节点，其它 DSP 照常工作。
  - 资源（IR、脉冲反馈、预设、Provider 文件）与 DSP 彻底分离，登记资源不影响执行计划。
- **自动化测试**：`tools/tests/dsp-host.test.js`（18 项，覆盖图谱编排、多节点共存、顺序、延迟、尾音、Provider 共存与隔离）与 `tools/tests/dsp-engine.test.js`（9 项，覆盖限幅器峰值保护、立体声联动、前瞻生效、参数平滑、引擎让位）。

## [1.0.0] - 2026-09-13

首个正式版本。

### 新增

- **项目基础**：基于 Echomusic 开源项目分支创建 RlonDSP，完整保留上游版权、作者信息与许可证。
- **Liquid Glass 视觉体系**：新增设计令牌文件 `glass-theme.css`（`--lg-*` 命名空间）、玻璃组件文件 `glass-components.css` 与播放栏动态高光脚本 `glass-motion.js`；玻璃材质按全局背景层 / 面板层 / 内容层 / 控制层 / 微玻璃分级，内容区保持高不透明度以保证文字清晰。
- **窗口形态**：无边框窗口与自定义标题栏，新增迷你窗口模式（360 × 64）。
- **实时音效**：AudioWorklet 实时 DSP 处理链，包含 9 段图示均衡器、总增益、压缩器、低音增强、立体声增强、虚拟环绕、清晰度增强、超高频净化、胆机模拟、FDN 混响、降噪、限幅，并支持预设的保存 / 删除 / 导出 / 导入。
- **脉冲反馈**：内嵌 RLONMUSIC DSP-IR 1200 脉冲响应生成器，13 级 DSP 链离线渲染，支持导出 16 / 24 / 32-bit WAV；新增脉冲列表，可保存多个自定义脉冲并支持启用、重命名、删除。
- **IRS 空间音效**：支持加载本地 WAV / AIFF 脉冲响应文件，实时卷积，支持干湿比、预延迟、高通、低通、A/B 对比、试听与旁路。
- **音频可视化**：由占位式频谱升级为 10 路专业可视化模块 —— 频谱分析仪、示波器、极坐标声场、响度历史、弧形电平表、倍频程频段、相位分析、动态范围、频谱质心、事件检测；共用每帧统一计算的音频度量，暂停时停止渲染并保留最后一帧。
- **桌面歌词**：独立的无边框、半透明、置顶歌词窗口，支持读取同名 `.lrc` 文件并逐行同步。
- **播放列表**：文件 / 文件夹 / 拖拽导入，搜索、排序、清空，曲目时长与封面解析。
- **系统集成**：系统托盘、媒体键、`Ctrl+Alt+P / ← / →` 全局快捷键、关闭时最小化到托盘。
- **多语言**：全部界面文字接入语言表，支持中文 / English 实时切换，包含画布内绘制的标注文字。
- **图标系统**：新增扁平化应用图标与内联 SVG 图标雪碧图，替换原有信号图标。
- **文档与测试**：新增 README、开源引用与鸣谢、贡献指南、架构说明、发布流程、GitHub 操作规范；新增冒烟测试（播放/暂停、切歌、音量、主题切换）。

### 变更

- 产品名称全局统一为 **RlonDSP**，窗口标题、程序名、关于页面、菜单栏全部替换，无旧名称残留。
- 标题栏简化为仅显示软件名称与窗口控制按钮，移除下方的重复名称与品牌区。
- 搜索框移至左侧播放列表顶部，并移除搜索框左侧的冗余功能按钮。
- 歌词入口由左侧文字选项改为底部播放控制栏的图标按钮。
- 右上角的主题切换与语言切换按钮移除，相关功能统一收敛到设置面板。
- 输出设备按钮移至设置弹出窗口内。
- 脉冲反馈的界面风格与整体设计语言统一，并移除底部说明横幅。
- 实时音效弹窗的布局、间距与二级卡片材质统一，内容不再截断。

### 修复

- 为界面元素获取函数增加缺失保护，避免元素被移除后出现白屏。
- 清理不再被引用的旧代码、样式与资源文件。

### 移除

- 移除瀑布图等旧可视化实现与配套配色表（已被新的 10 路可视化取代）。
- 移除已不再引用的 `src/icons.svg`（图标已内联到界面）。

---

[未发布]: https://github.com/EzRlon/RlonDSP/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/EzRlon/RlonDSP/releases/tag/v1.0.0
