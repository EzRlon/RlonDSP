# 贡献指南

感谢你有兴趣为 RlonDSP 做出贡献！无论是报告缺陷、提出建议，还是提交代码，都非常欢迎。

---

## 一、行为准则

- 保持友善与耐心，尊重不同水平的参与者。
- 讨论聚焦于技术问题本身，不针对个人。
- 提交内容需为原创，或已获得合法授权且与本项目许可证（GPL-3.0-only）兼容。

## 二、报告缺陷（Bug Report）

请在 [Issues](https://github.com/EzRlon/RlonDSP/issues) 中新建问题，并尽量包含：

1. **问题描述**：发生了什么，期望发生什么。
2. **复现步骤**：从启动程序开始，一步一动作地写清如何触发。
3. **环境信息**：Windows 版本（如 Windows 11 23H2）、程序版本（v1.0.0）、使用的是便携版还是安装版。
4. **音频信息**（音效类问题）：音频格式（MP3 / FLAC / WAV…）、采样率、是立体声还是单声道。
5. **日志与截图**：控制台报错信息或界面截图。截图请勿包含个人隐私内容（真实姓名、文件路径、私人歌名等）。

> 提示：按 `Ctrl + Shift + I` 可打开开发者工具查看控制台报错。

## 三、提出建议（Feature Request）

请说明：

- 你希望解决的实际问题是什么（而不是直接描述实现方案）；
- 该功能面向的场景（本地播放 / 音效处理 / 可视化分析）；
- 如果涉及音频处理，请说明是否会改变现有音质或破坏既有预设。

## 四、提交代码（Pull Request）

### 4.1 分支规范

从 `main` 分支创建你的工作分支，命名遵循以下前缀：

| 前缀 | 用途 | 示例 |
| :--- | :--- | :--- |
| `feature/` | 新功能 | `feature/spectrum-export` |
| `fix/` | 缺陷修复 | `fix/visualizer-freeze` |
| `docs/` | 文档更新 | `docs/readme-i18n` |
| `refactor/` | 不改变行为的重构 | `refactor/viz-metrics` |
| `chore/` | 构建、脚本、依赖维护 | `chore/update-tools` |

### 4.2 提交信息格式

采用 Conventional Commits 风格，使用中文或英文均可：

```text
<类型>: <简短描述>

<可选正文：为什么这样改、影响范围>
```

类型可选：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`chore`。

示例：

```text
fix: 修复频谱质心面板因变量遮蔽导致的渲染循环中断

局部变量 t 覆盖了 i18n 的翻译函数 t，绘制时抛出 TypeError，
导致 requestAnimationFrame 循环停止且无法自动恢复。
```

### 4.3 代码风格

- **界面层**：原生 HTML / CSS / JavaScript（ES2020+），无框架、无构建步骤。修改源码后刷新窗口即可生效。
- **缩进**：2 个空格；字符串默认使用单引号。
- **命名**：变量与函数用 `lowerCamelCase`，CSS 类用 `kebab-case`，设计令牌使用 `--lg-*` 命名空间。
- **样式**：界面颜色、模糊、圆角、阴影、动效时长一律引用 `--lg-*` 设计令牌，不要硬编码。
- **文本**：任何面向用户的文字都必须进入语言表（`I18N`），并通过 `t('键名')` 取用，同时补齐中文与英文。禁止在界面代码中直接写死可见文案。
- **注释**：文件头保留统一版权头；复杂算法（DSP、可视化度量计算）补充说明"为什么"。

### 4.4 必须遵守的红线

以下内容属于核心稳定性范围，改动前请先在 Issue 中讨论：

1. **音频链路**：`src/dsp-worklet.js` 的 DSP 处理链、`main.js` 的窗口配置、`preload.js` 的 IPC 接口，除非必要否则不要改动；如需新增 IPC，请先提交设计说明。
2. **上游合规**：不要删除或修改 `vendor/echomusic/` 下的版权声明、作者信息与许可证文件。
3. **不引入不兼容依赖**：本项目为 GPL-3.0-only，禁止引入与该许可证不兼容的依赖。
4. **保持离线**：不得引入云端 API、遥测、用户行为采集或任何形式的数据上传。
5. **不扩大权限**：不得新增访问用户磁盘其他区域的行为；本项目从不扫描、遍历用户磁盘。

### 4.5 提交前自测

请至少完成以下检查：

```bash
# 1. 语法检查（所有改动的 JS 文件）
node --check src/renderer.js
node --check src/glass-motion.js
node --check src/dsp-worklet.js
node --check main.js

# 2. 冒烟测试（播放/暂停、切歌、音量、主题切换）
node --test tools/tests/smoke.test.js
```

并手动确认：

- [ ] 程序可正常启动，控制台无报错；
- [ ] 导入音乐后播放、暂停、切歌、拖动进度、调节音量正常；
- [ ] 频谱可视化在播放时实时动起来，暂停时保持最后一帧；
- [ ] 实时音效与脉冲反馈弹窗可正常打开、关闭、调节参数；
- [ ] 中文 / English 切换后，界面文字全部跟随变化，无残留；
- [ ] 浅色 / 深色主题下均可正常阅读。

### 4.6 提交 PR

1. Fork 本仓库并推送到你自己的分支；
2. 向 `main` 分支发起 Pull Request；
3. 在描述中填写：改动目的、涉及文件、验证方式、已知影响；
4. 若 PR 关联某个 Issue，请使用 `Closes #编号` 形式关联；
5. 若改动包含界面变化，请附上前后对比截图。

## 五、开发环境

| 工具 | 说明 |
| :--- | :--- |
| Node.js | 18 及以上（仅开发与打包需要） |
| npm | 随 Node.js 提供 |
| 操作系统 | Windows 10 / 11 x64（打包目标平台） |

```bash
npm install          # 安装开发依赖
npm start            # 启动调试
npm run dist:portable  # 打包便携版
npm run dist:nsis      # 打包安装包
```

辅助脚本位于 `tools/`：

| 脚本 | 用途 |
| :--- | :--- |
| `tools/dev_eval.js` | 通过调试端口在运行中的窗口内执行表达式，便于验证界面状态 |
| `tools/tests/smoke.test.js` | 冒烟测试，自动启动隔离实例并验证四项核心功能 |
| `tools/make_icons.py` | 生成应用图标与托盘图标 |
| `tools/IconPatcher.exe` | 将图标嵌入主程序 exe |
| `tools/patch_pe.py` | 修补可执行文件的版本信息字符串 |
| `tools/scan_branding.py` | 扫描成品中的品牌残留 |
| `tools/make_test_audio.py` | 生成测试音频与测试歌词 |

## 六、文档与截图

- 文档位于 `docs/`，README 使用的截图存放于 `docs/images/`。
- 截图命名采用 `序号-用途.png`，例如 `01-main.png`、`13-realtime-effects.png`。
- 更新界面后请同步更新截图，避免文档与实物不符。
- 提交截图前请移除任何个人隐私信息（真实姓名、私人歌名、本机绝对路径等）。

## 七、许可证

向本项目提交贡献，即表示你同意你的贡献以 **GPL-3.0-only** 许可证授权，并同意项目在保留署名与许可证的前提下分发你的贡献。

<div align="center">

感谢你的贡献 ❤️

</div>
